# Development checkpoint — 20 September 2026

Historical record. Current instructions live in [policies](policies.md), [usage](usage-reference.md), and [setup](setup.md).

## Policy workspace

Added typed action rules, all/selected connection scopes, deterministic precedence, and restricted file-read approvals. A persistent draft holds edits until application; simulations are optional. History supports restore-as-draft, and pause/resume preserves the applied rules.

Past simulations retain up to 500 assessments; live simulations expose the latest 1,000. Results support outcome filters, search, pagination, saved evidence, and explanations of the winning rule. Unavailable evidence and unmatched requests remain explicit.

## Corrections

- Debug now precedes custom routing while retaining built-in prohibitions, incomplete-action checks, and deadlines.
- Relative time ranges roll forward consistently across charts and lists; explicit ranges remain fixed.
- Draft saves prevent conflicting edits without changing button appearance. Mobile Save/Test/Apply controls remain reachable.
- Conversation and Safety outcome coloring permit logarithmic scale. Raw counts remain unchanged; segment geometry is not proportional on that scale.

## Storage and verification

Migrations 0011–0014 added debug settings, policy history, persistent drafts/previews, and paused-rule retention. Local rollout used a database backup and checked for no outstanding assessments. Personal configuration was not committed.

At this checkpoint, 185 backend tests and 29 browser tests passed, along with TypeScript and the production build. The existing bundle-size warning remained. Tests used synthetic data and made no paid judge calls. Design and UI reviews covered scope, lifecycle, simulations, confirmations, and desktop/mobile behavior.

Single-host SQLite contention, shell-detection limits, and same-account tampering remained limitations. See [architecture](architecture.md#trade-offs).
